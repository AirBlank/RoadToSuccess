class Solution {
    public boolean divisorGame(int N) {
        //偶数先手必赢
        //奇数后手必赢
        if(N % 2 == 0)return true;
        return false;
    }
}

/*
最终结果应该是占到 2 的赢，占到 1 的输；
若当前为奇数，奇数的约数只能是奇数或者 1，因此下一个一定是偶数（奇数-奇数 或 奇数 - 1 都是偶数）；
若当前为偶数， 偶数的约数可以是奇数可以是偶数也可以是 1，因此直接减 1，则下一个是奇数；
因此，奇则输，偶则赢。
*/
